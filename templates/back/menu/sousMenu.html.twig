<ul>
    {% for menuPage in menuPages %}
        <li>
            <a href="#"><span id="{{ menuPage.page.id }}"></span>{{ menuPage.page.titre }}</a>
            {{ sousMenu(menuPage.page.id)|raw }}
        </li>
    {% endfor %}
</ul>